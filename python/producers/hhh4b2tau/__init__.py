"""Sub-modules of the HHH->4b2tau producer.

The analysis is HHH->4b2tau. It started from the HHH->6b framework, but the
object selection, categorisation, corrections and outputs have diverged far
enough that the package is named for what it actually reconstructs.
The orchestrator is ../hhh6bProducerPNetAK4_copy.py (module/class still carry
the historical 6b name -- renaming those touches every condor script,
launcher and skill, so it is deliberately left as a separate change).
"""
